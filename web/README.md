# CrystaLens Web

## Development

1. Install deps

```bash
pnpm i # or npm i / yarn
```

2. Run in dev

```bash
pnpm dev
```

Set env vars as needed:

- `NEXT_PUBLIC_API_HTTP` (default `http://localhost:8000`)
- `NEXT_PUBLIC_API_WS` (default `ws://localhost:8000`)
